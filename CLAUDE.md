# Project rules

- **Never use em dashes (—) anywhere in this project.** Not in website copy, not in code comments, not in commit messages. This is a hard rule with no exceptions. Rewrite the sentence with a comma, period, colon, semicolon, or parentheses instead.
